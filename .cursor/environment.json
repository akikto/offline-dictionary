{
  "name": "Shobdokosh Offline Dictionary",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "dev-server",
      "command": "export PATH=\"$HOME/.bun/bin:$PATH\" && bun run dev",
      "description": "Vite + Express dev server (tsx server.ts) on http://localhost:3000. The SPA is served under the /offline-dictionary/ base path."
    }
  ],
  "ports": [
    { "name": "dev-server", "port": 3000 }
  ]
}
